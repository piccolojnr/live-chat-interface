const components = {
    MuiButton: {
        styleOverrides: {
            root: {
                borderRadius: 8, // Custom border radius for buttons
            },
        },
    },
}

export default components;